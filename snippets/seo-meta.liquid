{%- comment -%}
  Empório Saibai — Tema Oficial
  Desenvolvido por Veltrus Growth & Technology
  Shopify Partner ID: 4969609
  Versão: 1.0.0 | 2025
  PROPRIEDADE INTELECTUAL PROTEGIDA
  Reprodução proibida sem autorização
{%- endcomment -%}
{%- liquid
  assign og_image = page_image
  if og_image == blank and product.featured_image
    assign og_image = product.featured_image
  elsif og_image == blank and settings.logo
    assign og_image = settings.logo
  endif
-%}
{%- capture saibai_meta_description -%}{% render 'saibai-meta-description' %}{%- endcapture -%}
{%- capture saibai_canonical_url -%}{% render 'saibai-canonical-url' %}{%- endcapture -%}
<meta property="og:site_name" content="{{ shop.name }}">
<meta property="og:title" content="{{ page_title | escape }}">
<meta property="og:description" content="{{ saibai_meta_description | strip | escape }}">
<meta property="og:url" content="{{ saibai_canonical_url | strip }}">
<meta property="og:locale" content="pt_BR">
<meta property="og:type" content="{% if template.name == 'product' %}product{% else %}website{% endif %}">
{%- if og_image -%}
  <meta property="og:image" content="{{ og_image | image_url: width: 1200 }}">
  <meta property="og:image:secure_url" content="{{ og_image | image_url: width: 1200 }}">
{%- endif -%}
<meta name="twitter:card" content="summary_large_image">
<meta name="twitter:title" content="{{ page_title | escape }}">
<meta name="twitter:description" content="{{ saibai_meta_description | strip | escape }}">
{%- if og_image -%}
  <meta name="twitter:image" content="{{ og_image | image_url: width: 1200 }}">
{%- endif -%}
